import 'package:flutter/material.dart';

class SwipeButtonWidget extends StatelessWidget {
  const SwipeButtonWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      //TODO: Build Swipe Button
    );
  }
}